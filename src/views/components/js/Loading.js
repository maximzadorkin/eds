import React from 'react'
import css from '../sass/Loading.module.sass'

const Loading = props =>
    <div className={css.section}>
        <div className={css.load}>
            &#xe02d;
        </div>
    </div>

export default Loading